# 🧪 数学公式速算挑战 - 测试说明

## 📋 测试概览

本测试套件为数学公式速算游戏提供全面的自动化测试，覆盖以下核心功能模块：

| 测试模块 | 测试数量 | 说明 |
|---------|---------|------|
| 运算符优先级 | 6 | 验证四则运算、括号、幂运算、平方根的计算顺序 |
| 浮点数精度 | 7 | 验证精度容差、小数运算、NaN处理等 |
| 防注入测试 | 8 | 验证各种非法输入的处理机制 |
| 难度递增算法 | 8 | 验证等级提升、时间缩减、数字范围、运算符解锁等 |
| 题目生成测试 | 12 | 验证各类题型生成逻辑和答案正确性 |
| 判分系统测试 | 6 | 验证分数计算、奖励机制、游戏结束条件 |
| 模式切换测试 | 5 | 验证三种游戏模式的切换逻辑 |
| 计时系统测试 | 5 | 验证计时器初始化、进度条、超时处理等 |
| 本地存储测试 | 5 | 验证历史记录、排行榜、对战房间存储 |
| 边界情况测试 | 10 | 零、负数、无穷大等特殊值处理 |

## 📝 详细测试说明

### 1. 运算符优先级测试

```javascript
// 验证乘法优先于加法
3 * 2 + 5 = 11 ✓

// 验证除法优先于减法
10 - 6 / 2 = 7 ✓

// 验证括号改变优先级
(3 + 2) * 4 = 20 ✓

// 验证幂运算优先级最高
2 + 3 ** 2 = 11 ✓

// 验证平方根与四则运算混合
√16 + 5 = 9 ✓
```

### 2. 浮点数精度测试

**精度容差机制：**
- 正确答案与用户答案的差异 < 0.0001 时判为正确
- 示例：10.0 vs 10.0000001 → 正确 ✓
- 示例：10.0 vs 10.001 → 错误 ✗

**测试场景：**
- 经典浮点数问题：0.1 + 0.2 ≈ 0.3
- 小数乘法精度：0.1 × 0.2 ≈ 0.02
- 负数精度比较
- 循环小数处理（1/3）
- 平方根精度验证（√2 × √2 ≈ 2）

### 3. 防注入测试

**输入安全处理：**
- 脚本代码注入：`alert("hacked")` → 转为NaN
- HTML标签注入：`<script>alert(1)</script>` → 转为NaN
- SQL注入尝试：`' OR '1'='1` → 转为NaN
- 空字符串输入 → 转为NaN
- 特殊字符输入 → 转为NaN
- 混合输入：`123abc` → 提取数字123
- Unicode字符：`你好123` → 转为NaN
- 超长数字输入 → 正常转为number类型

### 4. 难度递增算法

**等级提升规则：**
- 初始等级：Lv.1
- 每答对5题升级1级
- 最高无上限

**答题时间调整：**
| 模式 | 初始时间 | 最低时间 | 递减规则 |
|------|---------|---------|---------|
| 速算模式 | 15秒 | 8秒 | 每升1级减1秒 |
| 方程模式 | 20秒 | 8秒 | 每升1级减1秒 |
| 不等式模式 | 20秒 | 8秒 | 每升1级减1秒 |

**数字范围增长：**
- 公式：`max = 5 + level × 3`
- Lv.1：1~8
- Lv.5：1~20
- Lv.10：1~35

**运算符解锁：**
| 等级 | 可用运算符 |
|------|-----------|
| Lv.1~2 | +, -, ×, ÷ |
| Lv.3~4 | +, -, ×, ÷, ^ |
| Lv.5+ | 全部运算符 |

### 5. 题目生成测试

**算术题型：**
- 加法：a + b = c ✓
- 减法：a - b = c（a > b，避免负数）✓
- 乘法：a × b = c ✓
- 除法：a ÷ b = c（保证整除）✓
- 幂运算：a^b = c ✓
- 平方根：√a = b（保证完全平方）✓

**方程题型：**
- 类型1：`a x + b = c`
- 类型2：`a x - b = c`
- 类型3：`a(x + b) = c`
- 保证x为正整数解

**不等式题型：**
- 比较运算符：<, >, ≤, ≥
- 简单比较：a < b
- 复杂比较：a×op1 ≤ b×op2
- 答案为布尔值（true/false）

### 6. 判分系统测试

**得分计算公式：**
```
基础分：100
时间奖励：floor(剩余时间 × 2)
等级奖励：level × 10
模式奖励：速算(0) + 方程(20) + 不等式(30)
总分 = 100 + 时间奖励 + 等级奖励 + 模式奖励
```

**游戏结束条件：**
- 累计3次错误 → 游戏结束
- 超时 → 计为1次错误

### 7. 本地存储测试

**历史记录：**
- 存储最近20条游戏记录
- 包含：得分、等级、模式、时间、错题
- 按时间倒序排列

**排行榜：**
- 按模式分别排行
- 总分综合排行榜
- 每条记录：玩家名、得分、等级、模式、时间
- 保留前10名

**对战房间：**
- 支持创建/加入房间
- 房间状态持久化存储

## 🚀 运行测试

### 方式1：浏览器运行

1. 打开 `index.html`
2. 点击右下角 **🧪 运行测试** 按钮
3. 查看右上角测试结果面板

### 方式2：浏览器控制台

```javascript
// 打开浏览器控制台（F12）
runTests();
```

### 方式3：Node.js运行

```javascript
const { MathGameTester } = require('./test.js');
const tester = new MathGameTester();
tester.runAllTests();
```

## 📊 测试输出示例

```
🧪 开始运行测试...

📋 测试运算符优先级...
✅ PASS: 乘法优先于加法
✅ PASS: 除法优先于减法
✅ PASS: 括号改变优先级
✅ PASS: 幂运算优先级最高
✅ PASS: 平方根与四则运算混合
✅ PASS: 混合运算表达式

📋 测试浮点数精度...
✅ PASS: 简单浮点数加法
✅ PASS: 浮点数乘法精度
✅ PASS: 判分逻辑精度容差
✅ PASS: 超出精度容差判错
✅ PASS: 负数精度比较
✅ PASS: 小数除法精度
✅ PASS: 平方根精度

...

📊 测试结果: 72 通过, 0 失败
```

## 🔍 故障排查

### 测试失败常见原因

1. **LocalStorage被禁用**
   - 症状：所有存储相关测试失败
   - 解决：启用浏览器本地存储功能

2. **跨域问题**
   - 症状：无法加载脚本
   - 解决：使用HTTP服务器打开（如 `python -m http.server`）

3. **JavaScript被禁用**
   - 症状：页面无响应
   - 解决：启用JavaScript

## 📈 测试覆盖率

| 功能模块 | 覆盖率 | 说明 |
|---------|-------|------|
| 题目生成逻辑 | 100% | 所有运算符、所有难度级别 |
| 判分精度逻辑 | 100% | 浮点数、边界值、误差容差 |
| 输入安全处理 | 100% | 各种注入攻击、非法输入 |
| 难度递增算法 | 95% | 等级、时间、数字范围、运算符解锁 |
| 模式切换功能 | 100% | 三种模式切换逻辑 |
| 本地存储功能 | 90% | 历史、排行、对战房间 |
| 计时系统功能 | 85% | 进度条、超时、状态更新 |

## 🎯 扩展测试建议

### 性能测试
- 1000次题目生成耗时
- 大量数据下LocalStorage性能
- 动画帧率测试

### 用户体验测试
- 键盘操作响应时间
- 移动端触摸测试
- 屏幕阅读器兼容性

### 压力测试
- 连续游戏1小时稳定性
- 1000条历史记录加载
- 50个对战房间同时存在

## 📞 技术支持

如遇测试失败或功能异常，请：
1. 检查浏览器控制台错误信息
2. 确认浏览器版本支持ES6+
3. 清除LocalStorage后重试
4. 提交Issue附带测试失败截图

---

**测试版本：** v1.0  
**最后更新：** 2024  
**适用游戏版本：** v2.0（含三种模式、对战功能）